import 'package:flutter_starter_app/config.dart';
import 'package:flutter_starter_app/start_app.dart';

void main() {
  /// Here you can provide your app-specific config and
  /// therefore customize your app based on it.
  startApp(const AppConfig.staging());
}
